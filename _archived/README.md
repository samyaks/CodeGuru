# Archived Projects

These directories contain legacy or superseded code kept for reference only. They are **not** part of the active workspace.

- **code-reviewer/** — Original code-review app; functionality has been migrated into `app/`.
- **codebase-analyzer/** — Standalone analysis prototype; superseded by the analyzer in `app/`.
- **fastapi-project/** — Abandoned Python/FastAPI stub; never completed.
