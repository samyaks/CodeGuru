node_modules/
.git/
.env
.env.*
!.env.example
*.db
*.db-journal
*.db-wal
*.db-shm
data/
dist/
build/
.next/
.DS_Store
*.log

# Old sub-projects — not part of the deployed app.
# The legacy `_archived/` and `code-visualizer-mvp/{workspace-app,workspace-prototype,knowledge-graph-extension}` dirs
# were removed from main; these globs stay as a safety net in case they reappear locally.
_archived/
legacy/
code-reviewer/
codebase-analyzer/
code-visualizer-mvp/
fastapi-project/

# IDE / editor
.cursor/
.vscode/
*.swp
*.swo

# Docker compose (optional local files — not needed in image)
docker-compose*.yml
