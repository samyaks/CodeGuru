# .context.md — v2 component kit

## owner

@samyak (PM) · agent (initial build)

## purpose

The v2 design-system components rendered by `pages/v2/Project.tsx` and
`pages/v2/StyleGuide.tsx`. Mirrors the prototype at
`docs/v2-reference/takeoff-prototype.jsx`. All components are
presentational — no data fetching, no router calls.

## constraints

- Tailwind classes only. No inline styles except for dynamic widths
  (e.g. `ProgressBar`'s `width: ${value}%`).
- Use `stone-*`, `red-*`, `amber-*`, `emerald-*` palettes from Tailwind
  defaults. The v2 design intentionally diverges from the legacy
  `--color-brand` (sky-900) tokens used by v1 pages.
- Lucide icons only. Match prototype icon choices exactly.
- Use `v2-font-serif` / `v2-font-sans` classes from `styles/v2-tokens.css`
  rather than hardcoding `font-family`.
- Status enum is kebab-case (`'in-progress'`, not `'in_progress'`). The
  backend translates at the API boundary in
  `app/server/services/v2/gap-mapper.js`.
- Components must accept `className` and forward extra props where
  reasonable so the parent page can adjust spacing without forking.

## decisions

### Single barrel export at `components/v2/index.ts`
Every component + its exported types re-export through `index.ts`.
Pages import from `'../../components/v2'`. Keeps the import surface
small and lets us rename internal files without churning consumers.
agent · 2026-05-04

### `GapCard` owns refine-flow local state
`refining` and `refineText` live inside `GapCard`, not the parent. The
parent only sees the final `onRefine(id, instructions)` callback.
This keeps the gap list owner-agnostic — both `pages/v2/Project.tsx`
and the StyleGuide can render a gap without re-implementing the
refine textarea.
agent · 2026-05-04

## ai-log

### 2026-05-12 · cursor (claude-opus-4.7) — drop Ask Claude
Deleted `ChatDrawer.tsx`, removed its barrel exports, stripped the demo
from `StyleGuide.tsx`, and removed the "Ask Claude" CTA + drawer +
`STUB_REPLIES` from `Project.tsx`. No backend was wired to it; this is
pure dead-code cleanup.
Context: user request "drop ask claude section from the app".
Human modified: none.
Confidence: high.

### 2026-05-04 · cursor (claude-opus-4.7) — Phase 1
Generated all 10 v2 components from the prototype:
`Card`, `MetadataLabel`, `Badge`, `TabBar`, `ProgressBar`,
`EmptyState`, `PersonaCard`, `GapCard`, `ShippedItem`, `ChatDrawer`.
Plus `index.ts` barrel and the `/v2/style-guide` QA route.
(`ChatDrawer` later removed — see 2026-05-12 entry.)
Context: full prototype + plan provided.
Human modified: none yet.
Confidence: high.

## dependencies

Upstream: `lucide-react`, `react`, `tailwindcss`, the v2 tokens at
`app/client/src/styles/v2-tokens.css`.

Downstream: `app/client/src/pages/v2/Project.tsx`,
`pages/v2/StyleGuide.tsx`, `pages/v2/GapsSection.tsx`,
`pages/v2/ShippedSection.tsx`, `pages/v2/MapSection.tsx`,
`pages/v2/ContextSection.tsx`.

## status

Stage: shipped behind v2 flag. Style guide at `/v2/style-guide`.
Last reviewed: 2026-05-12
